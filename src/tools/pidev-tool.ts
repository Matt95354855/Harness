/**
 * Compatibility name from the original blueprint, not an official pi.dev integration.
 * An explicit generic or SearXNG endpoint must be supplied by the caller.
 */
export { HttpSearchProvider as PidevTool } from './search-tool.js';
export { HttpSearchProvider as default } from './search-tool.js';
